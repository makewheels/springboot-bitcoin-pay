package com.eg.testbitcoinpay.bitcoin;

import org.springframework.data.jpa.repository.JpaRepository;

public interface BitcoinAddressRepository extends JpaRepository<BitcoinAddressRepository, Integer> {
}
